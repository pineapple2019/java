package 创新大厦;

/**
 *  读取内容：
 *          next、nextLine、nextFloat这种是只读取一个元素，
 *          nextLine，是读取一行元素
 *  细节区别：
 *          next(),读取String类型，返回String类型
 *          nextInt()，输入int，返回int类型
 *          nextFloat()，读取float类型，返回float类型
 *          nextLine(),读取String类型，返回String类型
 *
 *  读取结束方式：
 *        nextLine是以换行为结束读取
 *        其余都是以空格和换行为结束读取
 *
 * @author pineapple
 */
public class Main {
    public static void main(String[] args) {

    }
}
